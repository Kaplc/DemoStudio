/**
 * HelloPlayerController — 外部工程根示例：玩家控制器
 * 示例不含输入绑定（Pawn 由 GameMode 的 Tick 直接驱动），仅演示最小子类。
 */
import { PlayerController } from '@/engine'

export class HelloPlayerController extends PlayerController {}