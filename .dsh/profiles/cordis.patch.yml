# DemoStudio 自定义配置补丁
# 将本地 .dsh\presets 目录添加为额外的 preset 根目录

- id: agent-presets
  name: '@deepseek-ai/dsh-agent-presets'
  config:
    default: standard
    roots:
      - path: "E:\\DemoStudio\\.dsh\\presets"
        trust: user
