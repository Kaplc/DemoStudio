---
name: user_ui_no_icon_by_default
description: UI 不加 icon/emoji，除非用户要求
type: user
---
UI 菜单/按钮默认不加 icon（emoji），只有用户明确要求时才加。

**Why:** 用户明确表示不喜欢默认带 icon，只有被要求时才加。

**How to apply:** 创建菜单项、按钮、面板标题等 UI 元素时，文本直接写中文名称，不加 emoji 前缀/后缀。
